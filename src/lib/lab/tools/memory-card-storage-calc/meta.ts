import type { LabToolMeta } from '@/lib/lab/types'

export const memoryCardStorageCalcMeta: LabToolMeta = {
  slug: 'memory-card-storage-calc',
  name: 'memory card storage calc',
  description:
    'Recording time from card size (decimal GB or GiB), decimal Mbps, optional bitrate presets, reserve headroom, and ~GB or GiB per hour.',
}
